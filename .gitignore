# macOS Finder metadata
.DS_Store

# local deployment config
.deploy.mk

# release artifacts
*.gz
*.bak
*.deploy
*.xml
*.xml.gz

# deployed MCP discovery (generated from template per-site)
.well-known/mcp.json

# AtomCode / InsCode local-scope configs (machine-specific)
.atomcode/
.inscode/

# cache directories (generated, environment-specific)
tldr_cache/
cache/

# site-specific config (contains API keys, paths)
phpman.config.php
*.swp
.claude/

# Composer (dev-only, not used in production — single-file deployment)
composer.json
composer.lock
vendor/
.phpman-test/

# placeholder leak (local dev runs before deploy patches PHPMAN_HOME)
__PHPMAN_HOME__/
